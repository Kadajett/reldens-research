{
  "name": "@reldens-community/utils-types",
  "version": "0.1.0",
  "description": "Zod schemas and inferred TypeScript types for @reldens/utils - the Reldens cross-cutting utilities (events, logging, validation, helpers). Hand-written from the source and drift-tested against the installed package.",
  "type": "commonjs",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "keywords": [
    "reldens",
    "utils",
    "zod",
    "types",
    "inventory",
    "trading",
    "mmorpg"
  ],
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test test/*.test.ts",
    "docs:api": "typedoc"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@reldens/utils": "0.54.0",
    "zod": "^4.4.3"
  },
  "reldens": {
    "typesFor": "@reldens/utils",
    "verifiedAgainst": "0.54.0"
  }
}
