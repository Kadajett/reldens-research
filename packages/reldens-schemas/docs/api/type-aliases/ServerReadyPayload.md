# Type Alias: ServerReadyPayload

```ts
type ServerReadyPayload = {
  serverManager: ServerManager;
};
```

`reldens.serverReady`
- Zone: server
- Dispatch: emit (awaited)
- Payload: one object with keys `serverManager`
- Emitted at: lib/game/server/manager.js:L280

## Properties

### serverManager

```ts
serverManager: ServerManager;
```
