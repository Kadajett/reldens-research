{
  "name": "reldens-research",
  "private": true,
  "version": "0.1.0",
  "description": "Tutorials, examples and docs for the Reldens MMORPG platform.",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "postinstall": "node scripts/link-reldens.mjs",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "link:reldens": "node scripts/link-reldens.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "reldens": "^4.0.0-beta.39.8"
  }
}
